services:
  - type: web
    name: req2testai-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
